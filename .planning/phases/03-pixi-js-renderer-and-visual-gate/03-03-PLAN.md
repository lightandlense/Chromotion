---
phase: 03-pixi-js-renderer-and-visual-gate
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified: []
autonomous: false
requirements:
  - INTEG-02
  - INTEG-03
  - INTEG-05

must_haves:
  truths:
    - "Ram renders flat-color reference scan (ram colored 2.png) with clean colors, no visible white gaps, no salmon artifacts"
    - "Real crayon strokes on paper are visible 1:1 in the animation — texture shows actual pencil/crayon grain, not a smoothed approximation"
    - "Russell confirms Track 2 output is visually cleaner than rigid_color_transfer.py on the same scan"
    - "Scaling decision is made: proceed with all 19 creatures OR stop and investigate"
  artifacts:
    - path: "data/scans/integ02_ref/"
      provides: "Reference flat-color scan processed textures"
    - path: "data/scans/integ03_real/"
      provides: "Real crayon scan processed textures"
  key_links:
    - from: "kiosk.html (browser)"
      to: "rigid_color_transfer.py output"
      via: "Side-by-side visual comparison — Russell opens both in browser windows"
      pattern: "visual comparison"
---

<objective>
Run the full kiosk path with real scan inputs (reference flat-color scan and a real crayon drawing), present the output to Russell for side-by-side comparison against rigid_color_transfer.py, and record the scaling decision.

Purpose: This is the go/no-go gate for the entire Track 2 investment. If Russell approves, all 19 creatures proceed through the offline bake pipeline. If not, the failure mode is diagnosed and architectural options are weighed before continuing.
Output: Russell's approval (or rejection with diagnosis) and a recorded decision about scaling to remaining creatures.
</objective>

<execution_context>
@C:/Users/Russell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Russell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:/Antigravity/Projects/Color Animals Interactive/.planning/ROADMAP.md
@E:/Antigravity/Projects/Color Animals Interactive/.planning/phases/03-pixi-js-renderer-and-visual-gate/03-01-SUMMARY.md
@E:/Antigravity/Projects/Color Animals Interactive/.planning/phases/03-pixi-js-renderer-and-visual-gate/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run reference flat-color scan through full kiosk path (INTEG-02)</name>
  <files>data/scans/integ02_ref/</files>
  <action>
Process the reference flat-color scan through the full kiosk pipeline and launch the kiosk browser for inspection.

Step 1 — Confirm real motion_data.json and rest_pose_masks are available:
- Read data/motion_data.json, check for `"__note"` field containing "SYNTHETIC"
- If SYNTHETIC: print warning "INTEG-02 will run with SYNTHETIC motion_data — animation will barely move. Visual gate requires real SAM2 bake (Phase 1 plan 01-04). Proceeding with renderer correctness check only."
- If real: proceed normally

Step 2 — Locate reference scan. Check these paths in order:
- src/animations/ram_colored_2.png
- "src/animations/ram colored 2.png"
- src/_color_samples/ (list files, use first PNG found)
If not found, use "src/Firefly ram walking 151585_colored_alpha_debug_scan.png" as substitute and note the substitution.

Step 3 — Run the full pipeline on the reference scan:
```
python src/preprocess/scan_rectify.py --input <ref_scan> --output data/scans/integ02_ref/rectified_scan.png
python src/preprocess/scan_slice.py --scan data/scans/integ02_ref/rectified_scan.png --masks-dir data/rest_pose_masks --output-dir data/scans/integ02_ref/textures
```
If scan_rectify fails (no ArUco markers in the flat-color reference image): copy the scan directly to data/scans/integ02_ref/rectified_scan.png and run scan_slice only. Note this in console output.

Step 4 — Start kiosk server: python ops/kiosk_server.py (in background, port 8000)

Step 5 — Trigger the scan via API so the kiosk loads integ02_ref textures:
```
curl -X POST http://localhost:8000/api/scan --data-binary @<ref_scan> -H "Content-Type: image/jpeg"
```
Then open http://localhost:8000/src/runtime/kiosk.html in browser. If curl POST triggers rectify rejection, use direct file copy approach and load textures via a different scan_id path.

Step 6 — Also produce rigid_color_transfer.py comparison output:
- Check rigid_color_transfer.py CLI signature first (--help or read the file)
- Run it on the same reference scan
- Save output to data/scans/integ02_comparison.png

Step 7 — Print clear instructions for Russell:
- URL to open for Track 2 kiosk view
- File path for Track 1 comparison PNG
- What to look for (salmon artifacts, white gaps, color accuracy)
  </action>
  <verify>
    <automated>cd "E:/Antigravity/Projects/Color Animals Interactive" && python -c "from pathlib import Path; textures = list(Path('data/scans/integ02_ref/textures').glob('*.png')) if Path('data/scans/integ02_ref/textures').exists() else []; print(f'Textures: {len(textures)}/8 parts'); exit(0 if len(textures) >= 8 else 1)"</automated>
  </verify>
  <done>8 texture PNG files exist in data/scans/integ02_ref/textures/, kiosk server is running, browser and comparison file instructions printed to console</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: INTEG-02 visual review — flat-color reference scan</name>
  <action>Human reviews Track 2 kiosk output vs Track 1 rigid_color_transfer comparison for the reference flat-color scan</action>
  <what-built>
    Reference flat-color scan (ram colored 2.png) processed through Track 2 pipeline and rendered in kiosk browser.
    Rigid_color_transfer.py comparison output also generated.
  </what-built>
  <how-to-verify>
    Step 1: Open the kiosk browser at http://localhost:8000/src/runtime/kiosk.html
    - The ram should animate with colors from the reference scan
    - Check: No white gaps between body parts during animation
    - Check: No salmon/pink artifacts in the texture rendering
    - Check: Line art composites cleanly on top with no flickering

    Step 2: Open data/scans/integ02_comparison.png (rigid_color_transfer.py output)
    - This is the Track 1 comparison output (expected: salmon artifacts, white gaps, hue shift)

    Step 3: Toggle between the kiosk animation and the comparison PNG

    Step 4: If motion_data is SYNTHETIC, the animation may barely move — this is expected.
    Visual quality of texture rendering (color accuracy, no artifacts) is still assessable.

    INTEG-02 pass criteria:
    - Track 2 kiosk output shows clean colors with no salmon/pink
    - Line art renders on top without gaps
    - Track 2 is visually cleaner than the comparison PNG on the same scan

    Type one of:
    - "approved-integ02" — INTEG-02 visual gate passes, continue to real scan
    - "fail: [describe what looks wrong]" — visual issue found, needs investigation
    - "synthetic-only: cannot assess" — motion_data is synthetic, cannot evaluate quality
  </how-to-verify>
  <verify>
    <automated>echo "Awaiting Russell approval"</automated>
  </verify>
  <done>Russell has typed "approved-integ02", "fail: ...", or "synthetic-only: cannot assess"</done>
  <resume-signal>Type "approved-integ02", "fail: [description]", or "synthetic-only: cannot assess"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run real crayon scan (INTEG-03 setup)</name>
  <files>data/scans/integ03_real/</files>
  <action>
Only proceed if the previous checkpoint was approved or "synthetic-only: cannot assess".

This task prepares the pipeline for a real paper scan of the ram template. The actual scan capture is done by Russell in the following checkpoint.

Step 1 — Verify kiosk webcam is accessible:
- Confirm the kiosk server is still running at localhost:8000
- Note for Russell: click "Scan" in kiosk.html — webcam preview should appear
- If webcam access appears blocked: diagnose (getUserMedia requires http:// not file://; user may need to grant camera permission in browser settings)

Step 2 — Print reminder about ArUco markers:
- The ram template must have 4 ArUco markers in corners for scan_rectify.py to work
- If Russell's printout lacks markers: bypass rectify and call scan_slice.py directly on the raw photo

Step 3 — Check for any existing real scan in the project:
- Look in data/scans/ for directories other than integ02_ref
- If a scan with real photographic content exists (not synthetic), use it as fallback INTEG-03 input

Step 4 — Pre-generate rigid_color_transfer.py comparison setup:
- After Russell's real scan is captured via kiosk (in next checkpoint), this will also run:
  python src/preprocess/rigid_color_transfer.py --scan <real_scan_rectified> --output data/scans/integ05_comparison.png
- Note the CLI signature to confirm it's correct

Step 5 — Print to console: "Ready for real scan capture at http://localhost:8000/src/runtime/kiosk.html — click Scan button to begin INTEG-03"
  </action>
  <verify>
    <automated>cd "E:/Antigravity/Projects/Color Animals Interactive" && python -c "import subprocess; result = subprocess.run(['python', 'ops/kiosk_server.py', '--help'], capture_output=True, timeout=3); print('Server script accessible')" 2>&1 | head -5; echo "Setup complete"</automated>
  </verify>
  <done>Kiosk is running and ready to accept a real webcam scan. Diagnostic output confirms webcam path or flags the issue. Console shows "Ready for real scan capture" message.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: INTEG-03 + INTEG-05 — real crayon scan and scaling decision</name>
  <action>Russell scans a real colored drawing, reviews Track 2 vs Track 1 output, and records the go/no-go scaling decision</action>
  <what-built>
    Kiosk is live at http://localhost:8000/src/runtime/kiosk.html and ready to process a real physical scan.
    Track 1 comparison output (rigid_color_transfer.py) will be generated from the same scan for side-by-side review.
  </what-built>
  <how-to-verify>
    INTEG-03: Real crayon scan test

    Step 1: Take a physical ram template printout and color it with crayons, colored pencils, or markers.
    (If no template available: use any colored drawing, or a phone photo of a colored object)

    Step 2: Hold it up to the webcam at http://localhost:8000/src/runtime/kiosk.html and click "Scan"
    (Alternative if webcam is unavailable:
     curl -X POST http://localhost:8000/api/scan --data-binary @drawing.jpg -H "Content-Type: image/jpeg")

    Step 3: Watch the ram animate with your actual crayon colors.

    INTEG-03 pass criteria:
    - Crayon strokes are VISIBLE in the animation — actual texture of crayon grain shows through
    - Colors appear 1:1, no hue shift
    - No warping of strokes (they appear in the correct location on the body part)
    - Animation loops cleanly

    INTEG-04 check (if your scan has outside-the-lines marks or dark patches):
    - Strokes outside lines are preserved (not removed)
    - Dark scribbled areas render as dark color (not as line art artifacts)
    - Uncolored white regions render white

    INTEG-05: Side-by-side comparison (scaling decision)

    Step 4: Check data/scans/integ05_comparison.png (Track 1 rigid_color_transfer.py on same scan)

    INTEG-05 approval criteria (you make the call):
    - Is Track 2 output (kiosk animation) visually cleaner than Track 1 (comparison PNG)?
    - Is the crayon texture preserved? (This is Track 2's core value proposition)
    - Is there any show-stopper that would prevent gallery deployment?

    Scaling decision:
    A) "approved: proceed with all 19 creatures" — Track 2 confirmed, scale up
    B) "approved: ram only for now" — Track 2 works, scale cautiously
    C) "fail: [describe issue]" — needs fix before scaling
    D) "fail: architectural change needed — [describe]"

    Type your decision:
  </how-to-verify>
  <verify>
    <automated>echo "Awaiting Russell approval"</automated>
  </verify>
  <done>Russell has provided an explicit scaling decision (approved or failed with diagnosis). Decision recorded in STATE.md and ROADMAP.md updated to reflect Phase 3 completion.</done>
  <resume-signal>Type "approved: [option]" or "fail: [description of issue]"</resume-signal>
</task>

</tasks>

<verification>
INTEG-05 gate is complete when Russell has provided an explicit approval or failure decision.

If approved: Update STATE.md Decisions with Russell's exact words. Update REQUIREMENTS.md INTEG-02/03/04/05 status to Complete. Update ROADMAP.md Phase 3 status to Complete with completion date.

If failed: Record failure reason and failing artifacts in STATE.md under Blockers/Concerns. The scaling decision remains deferred until the failure is addressed.
</verification>

<success_criteria>
- INTEG-02: Flat-color reference scan renders in kiosk with clean colors, no salmon artifacts, no white gaps — Russell confirms
- INTEG-03: Real crayon scan shows stroke texture 1:1 in animation without warping — Russell confirms
- INTEG-04: Bad scan behavior visually confirmed (strokes preserved, fallback white where uncolored) — observed during INTEG-03 scan
- INTEG-05: Russell provides explicit scaling decision recorded in STATE.md
- STATE.md and ROADMAP.md updated to reflect Phase 3 completion status
</success_criteria>

<output>
After completion, create E:/Antigravity/Projects/Color Animals Interactive/.planning/phases/03-pixi-js-renderer-and-visual-gate/03-03-SUMMARY.md

Record Russell's decision in STATE.md under Decisions:
- [Phase 03]: INTEG-05 gate — [approved/failed]: [Russell's exact words]
- [Phase 03]: Scaling decision — [option chosen]
</output>
